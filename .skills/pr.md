---
icon: "\U0001F680"
description: Format, commit, push, and open a PR
placeholder: optional PR title or target repo
---

Format the code (check pixi.toml, package.json, Makefile for the format command), then commit all changes with a descriptive message, push to remote, and open a PR. {args} Do NOT force push. If the branch doesn't have an upstream, push with -u.
