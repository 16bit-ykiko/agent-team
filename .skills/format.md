---
icon: "✨"
description: Run code formatter
---

Run the project's code formatter. Check pixi.toml, package.json, Makefile for the format command (e.g. pixi run format, npm run format, make format). Report what was changed.
