---
icon: "\U00002B06"
description: Push current branch to remote
placeholder: optional remote name
---

Push the current branch to remote. If the branch doesn't have an upstream yet, use `git push -u origin <branch>`. Do NOT force push. {args}
