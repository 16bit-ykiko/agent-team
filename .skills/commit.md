---
icon: "\U0001F4BE"
description: Format code and commit changes
placeholder: optional commit message hint
---

Format the code first (check pixi.toml, package.json, Makefile for the format command), then commit all current changes. {args} Write a concise, descriptive commit message based on the actual changes.
